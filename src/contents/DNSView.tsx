import React from 'react';
import { useNavigate } from 'react-router';
import { Form, Input, InputNumber, Button } from 'antd';

export default function DNSView(): JSX.Element {
  return (
    <>
        <h2>DNS 신청 조회</h2>
    </>
    );
  }