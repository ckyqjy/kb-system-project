import React from 'react';
import { useNavigate } from 'react-router';
import { Form, Input, InputNumber, Button } from 'antd';

export default function WebStorageView(): JSX.Element {
  return (
    <>
        <h2>웹 스토리지 신청 조회</h2>
    </>
    );
  }