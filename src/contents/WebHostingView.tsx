import React from 'react';
import { useNavigate } from 'react-router';
import { Form, Input, InputNumber, Button } from 'antd';

export default function WebHostingView(): JSX.Element {
  return (
    <>
        <h2>웹 호스팅 신청 조회</h2>
    </>
    );
  }